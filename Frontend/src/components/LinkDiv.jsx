import React, { useState } from "react";

const LinkDiv = ({ items, deleteUrl }) => {
  const [clicksIs, setClicksIs] = useState(items.click);
  console.log(items);
  return (
    
  );
};

export default LinkDiv;
